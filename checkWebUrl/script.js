 // const currentUrl = window.location.href;

    // console.log(currentUrl);

    function res() {


      const currentUrl = window.location.href;

      document.getElementById("output").innerText = `Current URL:${currentUrl}`;
    }